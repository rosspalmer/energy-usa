
# Postgres

- Make sure all read in tables land in `ingest` database and remove `public`
- Remove all Django tables
- There should be more required fields so nulls are not allowed when expected in ingest call

# Prefect

- Flow runs get stuck in `Running` state
- Flow runs need names should show indented range and cadence and be sortable
- Restarts on failed runs do not actually restart

# Django App

- This doesn't appear to work at all, remove web app completely for future rebuild
- Remove django packages

# Superset

- Automatically add all tables in `ingest` database as datasets

# Previous AI Code Issues

- Too many ingest Prefect flows silently return nothing or swallow errors
- Need to be more conscious of good data modeling practices
- Cursor stuff need to be converted to claude
