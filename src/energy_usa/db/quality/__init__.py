"""Quality audit database layer.

Submodules:
- db.quality.audit  — AuditRule / AuditResult dataclasses plus load/write/upsert helpers
- db.quality.checks — Generic SQL check functions (null rate, staleness, row count, …)
"""
