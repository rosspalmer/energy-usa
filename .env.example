# EIA API (required for /api/data/* and ingest flow). Get a key: https://www.eia.gov/opendata/register.php
EIA_API_KEY=
EIA_BASE_URL=https://api.eia.gov/v2

# Postgres (used by Docker Compose and ingest flow; optional for API-only local dev)
POSTGRES_USER=energy
POSTGRES_PASSWORD=energy
POSTGRES_DB=energy_usa
DATABASE_URL=postgresql://energy:energy@localhost:5432/energy_usa

# Optional: manager limits
# EIA_REQUEST_TIMEOUT_SECONDS=30
# EIA_MAX_CONCURRENT_REQUESTS=4
# EIA_MAX_RETRIES=3
